const globalStyle = `
  *, *::before, *::after {
    box-sizing: border-box;
  }
  body {
    background: #fbfbfb;
    color: rgba(0,0,0,.87);
    font-family: 'Open Sans', sans-serif;
  }

`

export default globalStyle
